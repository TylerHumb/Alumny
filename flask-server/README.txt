how to host python server locally
cd to flask-server folder

.env must be created with api_key_anthropic = valid anthoric api key

run: 
python server.py

you may need to install flask first
pip install flask

it should host to localhost:5000